{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch debugger",
            "type": "codelldb",
            "request": "launch",
            "cwd": "${workspaceFolder}/bin/",
            "program": "${workspaceFolder}/bin/testbed"
        }
    ]
}
